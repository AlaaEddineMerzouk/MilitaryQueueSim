# MilitaryQueueSim
A simple web application that simulates a military queue management system with priority handling based on ranks and mission types.  using React and Tailwind CSS, the interface supports Arabic and French languages with RTL support for Arabic. The system visually demonstrates the behavior of a queue (file) and a stack (pile) with prioritization.
